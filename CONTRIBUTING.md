If you want to suggest changes:
1. Write assembly code strictly for the x64 architecture.
2. Do not break the calling convention (Microsoft x64 calling convention).
3. Make sure the program compiles successfully in Visual Studio.
